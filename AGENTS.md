# Agent notes

- Test: `npm test` (runs `node --test`).
- Local dev server: `npx serve .`, then open `atlas.html` or `today.html`.
- No build step; content is versioned JSON under `content/`, validated against the schemas in the same folder.
